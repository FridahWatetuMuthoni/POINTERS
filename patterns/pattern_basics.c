#include <stdio.h>

/*
1. Check if the first column is the same or different
2. If is the same : j=common value esle: j=i
3. If the j values is increasing :j++ else : j--
4. check if the values of i associated with j are increasing or decreasing
*/

int main()
{
    return 0;
}